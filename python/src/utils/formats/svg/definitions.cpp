/**
 * @brief
 *
 * @file
 * @ingroup python
 */

#include <src/common.hpp>

#include "genesis/genesis.hpp"

using namespace ::genesis::utils;
/*
PYTHON_EXPORT_CLASS( ::genesis::utils::SvgDefinitions, scope )
{

    // -------------------------------------------------------------------
    //     Class SvgDefinitions
    // -------------------------------------------------------------------

    pybind11::class_< ::genesis::utils::SvgDefinitions, std::shared_ptr<::genesis::utils::SvgDefinitions> > ( scope, "SvgDefinitions" )
        .def(
            pybind11::init< T const & >(),
            pybind11::arg("object")
        )
        .def(
            pybind11::init< SvgDefinitions const & >(),
            pybind11::arg("other")
        )

        // Public Member Functions

        .def(
            "write",
            ( void ( ::genesis::utils::SvgDefinitions::* )( std::ostream &, size_t ) const )( &::genesis::utils::SvgDefinitions::write ),
            pybind11::arg("out"),
            pybind11::arg("indent")=(size_t)(0)
        )
    ;
}
*/
