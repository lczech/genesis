/**
 * @brief
 *
 * @file
 * @ingroup python
 */

#include <python/src/common.hpp>

#include "lib/genesis.hpp"

using namespace ::genesis::placement;

/*
PYTHON_EXPORT_CLASS (SimulatorSubtree, "placement")
{

    // -------------------------------------------------------------------
    //     Class SimulatorSubtree
    // -------------------------------------------------------------------

    boost::python::class_< ::genesis::placement::SimulatorSubtree > ( "SimulatorSubtree", boost::python::init< Sample & >(( boost::python::arg("smp") )) )

        // Public Member Functions

        .def(
            "generate",
            ( void ( ::genesis::placement::SimulatorSubtree::* )( size_t ))( &::genesis::placement::SimulatorSubtree::generate ),
            ( boost::python::arg("n") )
        )
    ;
}
*/
